<%= form_with model: [favorite, comment], data: { turbo: "true" } do |f| %>
  <div class="mb-2">
    <%= f.text_area :content, placeholder: "Add a comment...", class: "form-control" %>
  </div>
  <%= f.submit "Post Comment", class: "btn btn-sm btn-success" %>
<% end %>
